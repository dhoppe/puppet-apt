#
# THIS FILE IS MANAGED BY PUPPET
# <%= file %>
#

# Debian
deb http://<%= mirror_deb -%>/debian/ lenny main contrib non-free
deb-src http://<%= mirror_src -%>/debian/ lenny main contrib non-free

# Debian Security
deb http://<%= mirror_deb -%>/debian-security/ lenny/updates main contrib non-free
deb-src http://<%= mirror_src -%>/debian-security/ lenny/updates main contrib non-free

# Debian Volatile
deb http://<%= mirror_deb -%>/debian-volatile/ lenny/volatile main contrib non-free
deb-src http://<%= mirror_src -%>/debian-volatile/ lenny/volatile main contrib non-free

# Debian Backports
deb http://<%= mirror_deb -%>/debian-backports/ lenny-backports main contrib non-free
deb-src http://<%= mirror_src -%>/debian-backports/ lenny-backports main contrib non-free
