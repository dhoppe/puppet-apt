#
# THIS FILE IS MANAGED BY PUPPET
# <%= file %>
#

# Debian
deb http://<%= t_debian -%>/debian/ lenny main contrib non-free
deb-src http://<%= t_debian_src -%>/debian/ lenny main contrib non-free

# Debian Security
deb http://<%= t_debian -%>/debian-security/ lenny/updates main contrib non-free
deb-src http://<%= t_debian_src -%>/debian-security/ lenny/updates main contrib non-free

# Debian Volatile
deb http://<%= t_debian -%>/debian-volatile/ lenny/volatile main contrib non-free
deb-src http://<%= t_debian_src -%>/debian-volatile/ lenny/volatile main contrib non-free

# Debian Backports
deb http://<%= t_debian -%>/debian-backports/ lenny-backports main contrib non-free
deb-src http://<%= t_debian_src -%>/debian-backports/ lenny-backports main contrib non-free
