#
# THIS FILE IS MANAGED BY PUPPET
# <%= file %>
#

# Debian
deb http://<%= mirror_deb -%>/debian/ squeeze main contrib non-free
deb-src http://<%= mirror_src -%>/debian/ squeeze main contrib non-free

# Debian Security
deb http://<%= mirror_deb -%>/debian-security/ squeeze/updates main contrib non-free
deb-src http://<%= mirror_src -%>/debian-security/ squeeze/updates main contrib non-free

# Debian Volatile
deb http://<%= mirror_deb -%>/debian/ squeeze-updates main contrib non-free
deb-src http://<%= mirror_src -%>/debian/ squeeze-updates main contrib non-free

# Debian Backports
deb http://<%= mirror_deb -%>/debian-backports/ squeeze-backports main contrib non-free
deb-src http://<%= mirror_src -%>/debian-backports/ squeeze-backports main contrib non-free
